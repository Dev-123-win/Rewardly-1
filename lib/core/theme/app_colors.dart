import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primaryGreen = Color(0xFF00C853);
  static const secondaryBlue = Color(0xFF00B0FF);
  static const accentPurple = Color(0xFF8E24AA);

  // Background Colors
  static const background = Color(0xFFF9FAFB);

  // Surface Colors
  static const surface = Color(0xFFFFFFFF);

  // Error and Success Colors
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);

  // Gradient Colors
  static const gradientStart = primaryGreen;
  static const gradientMiddle = secondaryBlue;
  static const gradientEnd = accentPurple;

  // Text Colors
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF757575);

  // Card Color
  static const card = Color(0xFFFFFFFF);

  // Border Color
  static const border = Color(0xFFE0E0E0);
}
